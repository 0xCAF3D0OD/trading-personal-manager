{
  "name": "trading-personal-manager",
  "private": true,
  "version": "0.1.0",
  "description": "Dashboard personnel de surveillance on-chain de tokens Solana",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:api & npm run dev:web & wait",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/api && npm run typecheck -w apps/web && npm run typecheck -w apps/renderer && npm run typecheck -w apps/mcp",
    "test": "npm run test -w apps/api",
    "db:migrate": "npm run db:migrate -w apps/api"
  },
  "engines": {
    "node": ">=22.13"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.21.5": true,
    "vue-demi@0.14.10": true
  }
}
